{
  "name": "hima-web",
  "proxy": "https://server.himapersis.id",
  "private": true,
  "homepage": "http://himapersis.id",
  "main": "fe_server/index.js",
  "version": "0.1.0",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  },
  "scripts": {
    "start-server": "node fe_server/index.js",
    "start": "react-scripts start",
    "build": "react-scripts build&&node fe_server/index.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
