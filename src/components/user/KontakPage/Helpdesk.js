import React from "react";
import { SPubliserPage, STitle } from "./styles";

const Helpdesk = () => {
  return (
    <SPubliserPage>
      <STitle>Ini Helpdesk pages</STitle>
    </SPubliserPage>
  );
};

export default Helpdesk;
